---
interface Project {
  slug: string;
  title: string;
}

interface Props {
  currentSlug: string;
  projects: Project[];
}

const { currentSlug, projects } = Astro.props;
const otherProjects = projects.filter((p) => p.slug !== currentSlug);
---

<section aria-labelledby="more-studies-label" class="px-gutter max-w-content mx-auto">
  <div class="grid grid-cols-1 md:grid-cols-[1fr_3fr] gap-x-8 gap-y-2">
    <h2 id="more-studies-label" class="text-heading-lg font-medium leading-[1.4]">
      See more case studies
    </h2>
    <div class="flex flex-col gap-2">
      <a href="/" class="text-body-sm font-medium underline text-foreground">See all</a>
      {otherProjects.map((project) => (
        <a href={`/projects/${project.slug}`} class="text-body-sm text-muted hover:text-foreground leading-[1.5]">
          {project.title}
        </a>
      ))}
    </div>
  </div>
</section>
